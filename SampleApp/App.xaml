<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="SampleApp.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SampleApp"
    xmlns:converter="using:SampleApp.Converters">
    
    <Application.Resources>
        <ResourceDictionary>
            <converter:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <converter:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
            <converter:RoleToAlignmentConverter x:Key="RoleToAlignmentConverter"/>
            <converter:RoleToBackgroundConverter x:Key="RoleToBackgroundConverter"/>
            <converter:BubbleStyleByRole x:Key="BubbleStyleByRole"/>
            <SolidColorBrush x:Key="LightGray" Color="#FFD3D3D3"/>
            <SolidColorBrush x:Key="White" Color="#FFFFFFFF"/>
            <SolidColorBrush x:Key="Gray" Color= "#FF808080" />
            <SolidColorBrush x:Key="Transparent" Color= "#00FFFFFF" />
            <SolidColorBrush x:Key="WhiteSmoke" Color= "#FFF5F5F5" />
            <SolidColorBrush x:Key="Red" Color= "#FFFF0000" />
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!-- Other merged dictionaries here -->
            </ResourceDictionary.MergedDictionaries>
            <!-- Other app resources here -->
        </ResourceDictionary>
    </Application.Resources>
</Application>
